---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '6e9b7c66-7a63-4110-8901-1670ee3e68af'
  PropagateID: '6e9b7c66-7a63-4110-8901-1670ee3e68af'
  ReservedCode1: '7eae856e-8e83-4cad-807f-d8ab798208e2'
  ReservedCode2: '7eae856e-8e83-4cad-807f-d8ab798208e2'
---

# 权限控制 UI 可见性 — 4 种设计方案

> 目标：用户未配置权限时，对应菜单/页面/按钮不可见（而非点击后提示无权限）

---

## 方式一：命名约定法（零额外表）

### 核心思路

权限名直接对应路由名/菜单 key，无需额外映射表。通过 Laravel 原生 `@can` 指令和 `can:` 中间件实现 UI 隐藏 + 路由保护。

### 权限命名约定

```
模块.页面.动作

示例：
org.supplier.view     → 页面可见权限
org.supplier.create   → 新增按钮可见权限
org.supplier.edit     → 编辑按钮可见权限
org.supplier.delete   → 删除按钮可见权限
```

### 菜单配置文件

```php
// config/menu.php
return [
    [
        'key'   => 'org',
        'label' => '组织管理',
        'icon'  => 'building-office',
        'children' => [
            ['key' => 'org.supplier', 'label' => '供应商',   'permission' => 'org.supplier.view'],
            ['key' => 'org.merchant', 'label' => '商家',     'permission' => 'org.merchant.view'],
            ['key' => 'org.driver',   'label' => '司机',     'permission' => 'org.driver.view'],
            ['key' => 'org.vehicle',  'label' => '车辆',     'permission' => 'org.vehicle.view'],
            ['key' => 'org.route',    'label' => '配送线路', 'permission' => 'org.route.view'],
        ],
    ],
    [
        'key'   => 'product',
        'label' => '商品管理',
        'icon'  => 'cube',
        'children' => [
            ['key' => 'product.category', 'label' => '商品分类', 'permission' => 'product.category.view'],
            ['key' => 'product.sku',      'label' => '商品SKU',  'permission' => 'product.sku.view'],
        ],
    ],
];
```

### 菜单渲染组件

```php
{{-- resources/views/components/sidebar.blade.php --}}
@php
    $menuConfig = config('menu');
@endphp

<nav>
    @foreach($menuConfig as $group)
        @php
            $visibleChildren = collect($group['children'])
                ->filter(fn($item) => auth()->user()->can($item['permission']));
        @endphp

        {{-- 整组无权限则隐藏 --}}
        @if($visibleChildren->isNotEmpty())
            <div class="menu-group">
                <div class="menu-group-title">
                    <x-ui.icon :name="$group['icon']" />
                    {{ $group['label'] }}
                </div>
                @foreach($visibleChildren as $item)
                    <a href="{{ route($item['key'] . '.index') }}"
                       class="{{ request()->routeIs($item['key'] . '*') ? 'active' : '' }}">
                        {{ $item['label'] }}
                    </a>
                @endforeach
            </div>
        @endif
    @endforeach
</nav>
```

### 列表页按钮隐藏

```php
{{-- resources/views/livewire/org/supplier-list.blade.php --}}
@can('org.supplier.create')
    <button type="button" wire:click="openCreateModal">新增供应商</button>
@endcan

@foreach($suppliers as $supplier)
    <tr>
        <td>{{ $supplier->name }}</td>
        <td>
            @can('org.supplier.edit')
                <button type="button" wire:click="edit({{ $supplier->id }})">
                    <x-ui.icon name="pencil" />
                </button>
            @endcan

            @can('org.supplier.delete')
                <button type="button" wire:click="confirmDelete({{ $supplier->id }})">
                    <x-ui.icon name="trash" />
                </button>
            @endcan
        </td>
    </tr>
@endforeach
```

### 路由保护

```php
// routes/web.php
Route::middleware(['auth', 'can:org.supplier.view'])->group(function () {
    Route::get('/suppliers', [SupplierController::class, 'index'])->name('org.supplier.index');
});

Route::middleware(['auth', 'can:org.supplier.create'])->group(function () {
    Route::post('/suppliers', [SupplierController::class, 'store'])->name('org.supplier.store');
});

Route::middleware(['auth', 'can:org.supplier.edit'])->group(function () {
    Route::put('/suppliers/{supplier}', [SupplierController::class, 'update'])->name('org.supplier.update');
});

Route::middleware(['auth', 'can:org.supplier.delete'])->group(function () {
    Route::delete('/suppliers/{supplier}', [SupplierController::class, 'destroy'])->name('org.supplier.destroy');
});
```

### Seeder 示例

```php
// 已有 SystemDataSeeder 中的权限树即可，无需额外数据
// permissions 表中的 name 字段与路由名、菜单 permission key 对齐
```

### 优缺点

| 优点 | 缺点 |
|:---|:---|
| 零额外表，最简单 | 权限名必须严格对齐路由名/菜单 key |
| Laravel 原生 `@can` + `can:` 直接可用 | 改路由名要同步改权限名 |
| 性能最优，无额外查询 | 菜单结构硬编码在配置文件，需发版才能改 |

---

## 方式二：权限表扩展字段法

### 核心思路

在现有 `permissions` 表上增加路由名、菜单归属、图标、排序等字段，将权限和 UI 信息合并到一张表。

### 数据库变更

```php
// database/migrations/2026_08_01_000001_add_ui_fields_to_permissions_table.php
Schema::table('permissions', function (Blueprint $table) {
    $table->string('route_name')->nullable()->after('name')->comment('对应路由名');
    $table->string('menu_key')->nullable()->after('route_name')->comment('所属菜单标识');
    $table->string('menu_label')->nullable()->after('menu_key')->comment('菜单显示名');
    $table->string('icon')->nullable()->after('menu_label')->comment('菜单图标');
    $table->unsignedInteger('sort_order')->default(0)->after('icon')->comment('排序');
    $table->unsignedBigInteger('parent_id')->nullable()->after('sort_order')->comment('父级权限ID');
});
```

### 数据示例

```
| id | name               | type | route_name            | menu_key      | menu_label | icon    | sort_order | parent_id |
|----|--------------------|------|-----------------------|---------------|------------|---------|------------|-----------|
| 1  | org                | 1    | NULL                  | org           | 组织管理   | building| 1          | NULL      |
| 2  | org.supplier       | 2    | org.supplier.index    | org.supplier  | 供应商     | NULL    | 1          | 1         |
| 3  | org.supplier.view  | 3    | org.supplier.index    | NULL          | NULL       | NULL    | 0          | 2         |
| 4  | org.supplier.create| 3    | org.supplier.store    | NULL          | NULL       | NULL    | 0          | 2         |
| 5  | org.supplier.edit  | 3    | org.supplier.update   | NULL          | NULL       | NULL    | 0          | 2         |
| 6  | org.supplier.delete| 3    | org.supplier.destroy  | NULL          | NULL       | NULL    | 0          | 2         |
```

### 菜单查询逻辑

```php
// app/Services/MenuService.php
class MenuService
{
    public function getVisibleMenu(): Collection
    {
        $user = auth()->user();
        $permissionIds = $user->getAllPermissions()->pluck('id');

        return Permission::query()
            ->whereIn('id', $permissionIds)
            ->whereNotNull('menu_key')          // 只取有菜单标识的权限
            ->where('type', 2)                  // 页面级权限
            ->orderBy('sort_order')
            ->get()
            ->groupBy('parent_id');             // 按模块分组
    }

    public function getMenuTree(): Collection
    {
        $user = auth()->user();
        $permissionIds = $user->getAllPermissions()->pluck('id');

        // 模块级
        $modules = Permission::where('type', 1)->orderBy('sort_order')->get();

        return $modules->map(function ($module) use ($permissionIds) {
            $pages = Permission::query()
                ->where('parent_id', $module->id)
                ->where('type', 2)
                ->whereIn('id', $permissionIds)
                ->whereNotNull('menu_key')
                ->orderBy('sort_order')
                ->get();

            if ($pages->isEmpty()) {
                return null;
            }

            return [
                'key'      => $module->menu_key,
                'label'    => $module->menu_label,
                'icon'     => $module->icon,
                'children' => $pages->map(fn($p) => [
                    'key'   => $p->menu_key,
                    'label' => $p->menu_label,
                    'route' => $p->route_name,
                ])->values(),
            ];
        })->filter()->values();
    }
}
```

### 菜单渲染

```php
@php
    $menuTree = app(\App\Services\MenuService::class)->getMenuTree();
@endphp

<nav>
    @foreach($menuTree as $group)
        <div class="menu-group">
            <div class="menu-group-title">
                <x-ui.icon :name="$group['icon']" />
                {{ $group['label'] }}
            </div>
            @foreach($group['children'] as $item)
                <a href="{{ route($item['route']) }}"
                   class="{{ request()->routeIs($item['key'] . '*') ? 'active' : '' }}">
                    {{ $item['label'] }}
                </a>
            @endforeach
        </div>
    @endforeach
</nav>
```

### 按钮隐藏

```php
{{-- 仍然使用 @can，但权限名从数据库获取 --}}
@can('org.supplier.create')
    <button type="button" wire:click="openCreateModal">新增供应商</button>
@endcan
```

### 路由保护

```php
// 使用中间件，权限名与数据库 name 字段对齐
Route::middleware(['auth', 'can:org.supplier.view'])->group(function () {
    Route::get('/suppliers', [SupplierController::class, 'index'])->name('org.supplier.index');
});
```

### 优缺点

| 优点 | 缺点 |
|:---|:---|
| 一张表搞定，查询简单 | permissions 表职责变重 |
| 运行时可动态修改菜单（改数据库即可） | button 级权限的 menu_key/route_name 为空，有冗余字段 |
| 菜单排序/图标可配置 | 需 Migration + 改 Model fillable |
| 权限和 UI 信息强关联，不会错位 | 多角色同权限时菜单信息重复存储 |

---

## 方式三：独立映射表法

### 核心思路

新建 `permission_ui` 表，将权限与 UI 元素（菜单/页面/按钮）解耦，一个权限可映射多个 UI 元素。

### 数据库变更

```php
// database/migrations/2026_08_01_000001_create_permission_ui_table.php
Schema::create('permission_ui', function (Blueprint $table) {
    $table->id();
    $table->foreignId('permission_id')->constrained()->cascadeOnDelete()->comment('关联权限ID');
    $table->enum('element_type', ['menu', 'page', 'button'])->comment('UI元素类型');
    $table->string('element_key')->comment('元素标识（如 org.supplier）');
    $table->string('element_label')->comment('显示名称');
    $table->string('route_name')->nullable()->comment('对应路由名');
    $table->string('parent_key')->nullable()->comment('父级元素key');
    $table->string('icon')->nullable()->comment('图标');
    $table->unsignedInteger('sort_order')->default(0)->comment('排序');
    $table->timestamps();

    $table->unique(['permission_id', 'element_type', 'element_key']);
});
```

### 数据示例

```
| id | permission_id | element_type | element_key         | element_label | route_name           | parent_key | icon    | sort_order |
|----|---------------|-------------|---------------------|---------------|----------------------|------------|---------|------------|
| 1  | 1(org)        | menu        | org                 | 组织管理      | NULL                 | NULL       | building| 1          |
| 2  | 2(org.supplier)| page       | org.supplier        | 供应商        | org.supplier.index   | org        | NULL    | 1          |
| 3  | 3(.view)      | button      | org.supplier.create | 新增          | NULL                 | org.supplier| NULL   | 1          |
| 4  | 4(.create)    | button      | org.supplier.edit   | 编辑          | NULL                 | org.supplier| NULL   | 2          |
| 5  | 5(.edit)      | button      | org.supplier.delete | 删除          | NULL                 | org.supplier| NULL   | 3          |
| 6  | 6(.delete)    | button      | org.merchant.create | 新增          | NULL                 | org.merchant| NULL   | 1          |
```

### Model

```php
// app/Models/PermissionUi.php
class PermissionUi extends Model
{
    protected $table = 'permission_ui';

    protected $fillable = [
        'permission_id', 'element_type', 'element_key',
        'element_label', 'route_name', 'parent_key',
        'icon', 'sort_order',
    ];

    public function permission(): BelongsTo
    {
        return $this->belongsTo(Permission::class);
    }
}
```

### 菜单服务

```php
// app/Services/MenuService.php
class MenuService
{
    public function getVisibleMenu(): Collection
    {
        $user = auth()->user();
        $permissionIds = $user->getAllPermissions()->pluck('id');

        $uiElements = PermissionUi::query()
            ->whereIn('permission_id', $permissionIds)
            ->orderBy('sort_order')
            ->get();

        // 构建模块级菜单
        $modules = $uiElements->where('element_type', 'menu');
        $pages   = $uiElements->where('element_type', 'page');

        return $modules->map(function ($module) use ($pages) {
            $children = $pages->where('parent_key', $module->element_key);

            if ($children->isEmpty()) {
                return null;
            }

            return [
                'key'      => $module->element_key,
                'label'    => $module->element_label,
                'icon'     => $module->icon,
                'children' => $children->map(fn($p) => [
                    'key'   => $p->element_key,
                    'label' => $p->element_label,
                    'route' => $p->route_name,
                ])->values(),
            ];
        })->filter()->values();
    }

    public function getVisibleButtons(string $pageKey): Collection
    {
        $user = auth()->user();
        $permissionIds = $user->getAllPermissions()->pluck('id');

        return PermissionUi::query()
            ->whereIn('permission_id', $permissionIds)
            ->where('element_type', 'button')
            ->where('parent_key', $pageKey)
            ->orderBy('sort_order')
            ->pluck('element_key');
    }
}
```

### 菜单渲染

```php
@php
    $menuTree = app(\App\Services\MenuService::class)->getVisibleMenu();
@endphp

<nav>
    @foreach($menuTree as $group)
        <div class="menu-group">
            <div class="menu-group-title">
                <x-ui.icon :name="$group['icon']" />
                {{ $group['label'] }}
            </div>
            @foreach($group['children'] as $item)
                <a href="{{ route($item['route']) }}"
                   class="{{ request()->routeIs($item['key'] . '*') ? 'active' : '' }}">
                    {{ $item['label'] }}
                </a>
            @endforeach
        </div>
    @endforeach
</nav>
```

### 按钮渲染

```php
@php
    $visibleButtons = app(\App\Services\MenuService::class)->getVisibleButtons('org.supplier');
@endphp

@if($visibleButtons->contains('org.supplier.create'))
    <button type="button" wire:click="openCreateModal">新增供应商</button>
@endif

@foreach($suppliers as $supplier)
    <tr>
        <td>{{ $supplier->name }}</td>
        <td>
            @if($visibleButtons->contains('org.supplier.edit'))
                <button type="button" wire:click="edit({{ $supplier->id }})">
                    <x-ui.icon name="pencil" />
                </button>
            @endif

            @if($visibleButtons->contains('org.supplier.delete'))
                <button type="button" wire:click="confirmDelete({{ $supplier->id }})">
                    <x-ui.icon name="trash" />
                </button>
            @endif
        </td>
    </tr>
@endforeach
```

### 路由保护

```php
// 仍使用 Laravel 原生中间件
Route::middleware(['auth', 'can:org.supplier.view'])->group(function () {
    Route::get('/suppliers', [SupplierController::class, 'index'])->name('org.supplier.index');
});
```

### 优缺点

| 优点 | 缺点 |
|:---|:---|
| 解耦最彻底，权限和 UI 独立维护 | 多一张表 + 一层关联查询 |
| 一个权限可映射多个 UI 元素（如一个权限控制2个按钮） | Seeder 维护成本增加 |
| UI 元素可独立增删，不影响权限逻辑 | 查询时需要 join 或两次查询 |
| 运行时完全可动态配置 | 复杂度最高 |

---

## 方式四：配置文件法

### 核心思路

在 `config/` 中定义完整的菜单结构 + 权限映射，不入库。渲染时通过 `@can` 过滤。

### 菜单配置文件

```php
// config/menu.php
return [
    [
        'key'   => 'org',
        'label' => '组织管理',
        'icon'  => 'building-office',
        'children' => [
            [
                'key'        => 'org.supplier',
                'label'      => '供应商',
                'route'      => 'org.supplier.index',
                'permission' => 'org.supplier.view',
                'buttons'    => [
                    ['key' => 'create', 'permission' => 'org.supplier.create', 'label' => '新增'],
                    ['key' => 'edit',   'permission' => 'org.supplier.edit',   'label' => '编辑'],
                    ['key' => 'delete', 'permission' => 'org.supplier.delete',  'label' => '删除'],
                ],
            ],
            [
                'key'        => 'org.merchant',
                'label'      => '商家',
                'route'      => 'org.merchant.index',
                'permission' => 'org.merchant.view',
                'buttons'    => [
                    ['key' => 'create', 'permission' => 'org.merchant.create', 'label' => '新增'],
                    ['key' => 'edit',   'permission' => 'org.merchant.edit',   'label' => '编辑'],
                    ['key' => 'delete', 'permission' => 'org.merchant.delete',  'label' => '删除'],
                ],
            ],
            [
                'key'        => 'org.driver',
                'label'      => '司机',
                'route'      => 'org.driver.index',
                'permission' => 'org.driver.view',
            ],
            [
                'key'        => 'org.vehicle',
                'label'      => '车辆',
                'route'      => 'org.vehicle.index',
                'permission' => 'org.vehicle.view',
            ],
            [
                'key'        => 'org.route',
                'label'      => '配送线路',
                'route'      => 'org.route.index',
                'permission' => 'org.route.view',
            ],
        ],
    ],
    [
        'key'   => 'product',
        'label' => '商品管理',
        'icon'  => 'cube',
        'children' => [
            [
                'key'        => 'product.category',
                'label'      => '商品分类',
                'route'      => 'product.category.index',
                'permission' => 'product.category.view',
            ],
            [
                'key'        => 'product.sku',
                'label'      => '商品SKU',
                'route'      => 'product.sku.index',
                'permission' => 'product.sku.view',
            ],
        ],
    ],
];
```

### 菜单渲染组件

```php
{{-- resources/views/components/sidebar.blade.php --}}
@php
    $menuConfig = config('menu');
@endphp

<nav>
    @foreach($menuConfig as $group)
        @php
            $visibleChildren = collect($group['children'])
                ->filter(fn($item) => auth()->user()->can($item['permission']));
        @endphp

        {{-- 整组无权限则隐藏 --}}
        @if($visibleChildren->isNotEmpty())
            <div class="menu-group">
                <div class="menu-group-title">
                    <x-ui.icon :name="$group['icon']" />
                    {{ $group['label'] }}
                </div>
                @foreach($visibleChildren as $item)
                    <a href="{{ route($item['route']) }}"
                       class="{{ request()->routeIs($item['key'] . '*') ? 'active' : '' }}">
                        {{ $item['label'] }}
                    </a>
                @endforeach
            </div>
        @endif
    @endforeach
</nav>
```

### 按钮渲染（读取配置）

```php
{{-- resources/views/livewire/org/supplier-list.blade.php --}}
@php
    $menuConfig = collect(config('menu'))->keyBy('key');
    $currentPage = $menuConfig->get('org')['children'] ?? [];
    $pageConfig  = collect($currentPage)->keyBy('key')->get('org.supplier');
    $buttons     = collect($pageConfig['buttons'] ?? []);
@endphp

@if($buttons->where('permission', 'org.supplier.create')->isNotEmpty())
    @can('org.supplier.create')
        <button type="button" wire:click="openCreateModal">新增供应商</button>
    @endcan
@endif

@foreach($suppliers as $supplier)
    <tr>
        <td>{{ $supplier->name }}</td>
        <td>
            @can('org.supplier.edit')
                <button type="button" wire:click="edit({{ $supplier->id }})">
                    <x-ui.icon name="pencil" />
                </button>
            @endcan

            @can('org.supplier.delete')
                <button type="button" wire:click="confirmDelete({{ $supplier->id }})">
                    <x-ui.icon name="trash" />
                </button>
            @endcan
        </td>
    </tr>
@endforeach
```

### 路由保护

```php
// 与方式一相同，使用 Laravel 原生中间件
Route::middleware(['auth', 'can:org.supplier.view'])->group(function () {
    Route::get('/suppliers', [SupplierController::class, 'index'])->name('org.supplier.index');
});
```

### 优缺点

| 优点 | 缺点 |
|:---|:---|
| 无额外表，零数据库查询 | 菜单结构改了需发版 |
| 配置文件版本管理友好 | 运行时不可动态修改菜单 |
| Laravel 原生 `@can` 直接可用 | 按钮权限配置与页面耦合在同一个数组中 |
| 开发直观，所见即所得 | 多环境菜单差异需额外处理 |

---

## 四种方式对比总结

| 维度 | 方式一·命名约定 | 方式二·扩展字段 | 方式三·独立映射表 | 方式四·配置文件 |
|:---|:---|:---|:---|:---|
| **额外数据表** | 无 | 无（改现有表） | 1 张新表 | 无 |
| **菜单来源** | 配置文件 | 数据库查询 | 数据库查询 | 配置文件 |
| **按钮来源** | Blade 硬编码 `@can` | Blade 硬编码 `@can` | 数据库查询 | 配置文件 |
| **运行时可配置** | 否 | 是 | 是 | 否 |
| **数据库查询次数** | 0（权限已缓存） | 1 次 | 1~2 次 | 0 |
| **权限-UI 耦合度** | 高（命名强绑定） | 中（同表不同字段） | 低（独立表解耦） | 高（配置文件绑定） |
| **Migration 改动** | 无 | 加 5~6 字段 | 新建 1 表 | 无 |
| **Seeder 维护** | 无需额外 | 需填充新字段 | 需填充映射表 | 无需额外 |
| **菜单改排序/图标** | 改配置文件 + 发版 | 改数据库即可 | 改数据库即可 | 改配置文件 + 发版 |
| **一个权限控制多 UI** | 不支持 | 不支持 | 支持 | 不支持 |
| **适合团队规模** | 1~3 人小团队 | 3~5 人中型 | 5+ 人大型/复杂业务 | 1~3 人小团队 |
| **与 Spatie 兼容性** | 完美 | 需改 Model | 完美 | 完美 |

### 各方式 UI 隐藏效果（4 种完全一致）

| 层级 | 实现手段 | 无权限时表现 |
|:---|:---|:---|
| 菜单 | `@can` / 权限查询过滤 | 菜单不可见，源码无输出 |
| 页面 | `can:` 路由中间件 | 直接访问 URL 返回 403 |
| 按钮 | `@can` / 权限查询过滤 | 按钮不可见，源码无输出 |

> 4 种方式均实现 **UI 隐藏**而非"点击后提示无权限"。区别仅在于权限与 UI 元素的映射关系存储位置不同。

---

## 推荐方案：方式一 + 方式四 组合

### 核心思路

- **菜单结构**由配置文件定义（方式四），集中管理，清晰直观
- **权限名与路由名**严格命名约定对齐（方式一），Blade 直接用 `@can` / `can:` 中间件
- **零额外表**，完全依赖 Spatie Permission + Laravel 原生能力

### 设计原则

```
1. 权限名 = 模块.页面.动作（如 org.supplier.create）
2. 路由名 = 权限名去掉动作部分 + 路由动作（如 org.supplier.store）
3. 菜单 permission = 页面查看权限（如 org.supplier.view）
4. 按钮 @can = 按钮动作权限（如 org.supplier.create）
5. 路由 can: = 页面/按钮权限（如 can:org.supplier.view）
```

### 完整示例

#### 1. 权限命名约定表

```
| 权限名               | type | 对应路由中间件         | UI 用途       |
|----------------------|------|------------------------|---------------|
| org                  | 1    | —                      | 模块分组      |
| org.supplier         | 2    | —                      | 菜单项        |
| org.supplier.view    | 3    | can:org.supplier.view  | 页面访问      |
| org.supplier.create  | 3    | can:org.supplier.create| 新增按钮可见  |
| org.supplier.edit    | 3    | can:org.supplier.edit  | 编辑按钮可见  |
| org.supplier.delete  | 3    | can:org.supplier.delete| 删除按钮可见  |
```

> 注意：type=1 模块和 type=2 页面级权限用于菜单分组，type=3 按钮级权限用于 `@can` 控制按钮可见性。实际拦截靠 type=3 的 `can:` 中间件。

#### 2. 菜单配置文件

```php
// config/menu.php
return [
    [
        'key'   => 'org',
        'label' => '组织管理',
        'icon'  => 'building-office',
        'children' => [
            [
                'key'        => 'org.supplier',
                'label'      => '供应商',
                'route'      => 'org.supplier.index',
                'permission' => 'org.supplier.view',
            ],
            [
                'key'        => 'org.merchant',
                'label'      => '商家',
                'route'      => 'org.merchant.index',
                'permission' => 'org.merchant.view',
            ],
            [
                'key'        => 'org.driver',
                'label'      => '司机',
                'route'      => 'org.driver.index',
                'permission' => 'org.driver.view',
            ],
            [
                'key'        => 'org.vehicle',
                'label'      => '车辆',
                'route'      => 'org.vehicle.index',
                'permission' => 'org.vehicle.view',
            ],
            [
                'key'        => 'org.route',
                'label'      => '配送线路',
                'route'      => 'org.route.index',
                'permission' => 'org.route.view',
            ],
        ],
    ],
    [
        'key'   => 'product',
        'label' => '商品管理',
        'icon'  => 'cube',
        'children' => [
            [
                'key'        => 'product.category',
                'label'      => '商品分类',
                'route'      => 'product.category.index',
                'permission' => 'product.category.view',
            ],
            [
                'key'        => 'product.sku',
                'label'      => '商品SKU',
                'route'      => 'product.sku.index',
                'permission' => 'product.sku.view',
            ],
        ],
    ],
    [
        'key'   => 'system',
        'label' => '系统管理',
        'icon'  => 'cog-6-tooth',
        'children' => [
            [
                'key'        => 'system.user',
                'label'      => '用户管理',
                'route'      => 'system.user.index',
                'permission' => 'system.user.view',
            ],
            [
                'key'        => 'system.role',
                'label'      => '角色管理',
                'route'      => 'system.role.index',
                'permission' => 'system.role.view',
            ],
            [
                'key'        => 'system.permission',
                'label'      => '权限管理',
                'route'      => 'system.permission.index',
                'permission' => 'system.permission.view',
            ],
        ],
    ],
];
```

#### 3. 侧边栏组件

```blade
{{-- resources/views/components/layout/sidebar.blade.php --}}
@php
    $menuConfig = config('menu');
@endphp

<aside class="sidebar">
    @foreach($menuConfig as $group)
        @php
            $visibleChildren = collect($group['children'])
                ->filter(fn($item) => auth()->user()->can($item['permission']));
        @endphp

        @if($visibleChildren->isNotEmpty())
            <div class="menu-group" x-data="{ open: true }">
                <button type="button"
                        class="menu-group-title"
                        @click="open = !open">
                    <x-ui.icon :name="$group['icon']" class="w-5 h-5" />
                    <span>{{ $group['label'] }}</span>
                    <x-ui.icon name="chevron-down" class="w-4 h-4 ml-auto transition-transform" :class="{ 'rotate-180': open }" />
                </button>

                <div x-show="open" x-transition>
                    @foreach($visibleChildren as $item)
                        <a href="{{ route($item['route']) }}"
                           class="menu-item {{ request()->routeIs($item['key'] . '*') ? 'active' : '' }}">
                            {{ $item['label'] }}
                        </a>
                    @endforeach
                </div>
            </div>
        @endif
    @endforeach
</aside>
```

#### 4. 列表页按钮控制（统一模式）

```blade
{{-- resources/views/livewire/org/supplier-list.blade.php --}}
<div>
    {{-- 工具栏：仅新增按钮受权限控制 --}}
    <div class="toolbar">
        @can('org.supplier.create')
            <button type="button" wire:click="openCreateModal" class="btn-primary">
                新增供应商
            </button>
        @endcan
    </div>

    {{-- 表格 --}}
    <table>
        <thead>
            <tr>
                <th>名称</th>
                <th>结算周期</th>
                <th>状态</th>
                @canany(['org.supplier.edit', 'org.supplier.delete'])
                    <th>操作</th>
                @endcanany
            </tr>
        </thead>
        <tbody>
            @foreach($suppliers as $supplier)
                <tr>
                    <td>{{ $supplier->name }}</td>
                    <td>{{ $supplier->settlement_cycle }}</td>
                    <td>{{ $supplier->status }}</td>
                    @canany(['org.supplier.edit', 'org.supplier.delete'])
                        <td>
                            @can('org.supplier.edit')
                                <button type="button" wire:click="edit({{ $supplier->id }})">
                                    <x-ui.icon name="pencil" class="w-3.5 h-3.5" />
                                </button>
                            @endcan
                            @can('org.supplier.delete')
                                <button type="button" wire:click="confirmDelete({{ $supplier->id }})">
                                    <x-ui.icon name="trash" class="w-3.5 h-3.5" />
                                </button>
                            @endcan
                        </td>
                    @endcanany
                </tr>
            @endforeach
        </tbody>
    </table>
</div>
```

#### 5. 路由保护

```php
// routes/web.php

// 组织管理
Route::middleware(['auth', 'verified'])->group(function () {
    // 供应商
    Route::middleware('can:org.supplier.view')->group(function () {
        Route::get('/suppliers', SupplierList::class)->name('org.supplier.index');
    });
    Route::middleware('can:org.supplier.create')->group(function () {
        Route::post('/suppliers', [SupplierController::class, 'store'])->name('org.supplier.store');
    });
    Route::middleware('can:org.supplier.edit')->group(function () {
        Route::put('/suppliers/{supplier}', [SupplierController::class, 'update'])->name('org.supplier.update');
    });
    Route::middleware('can:org.supplier.delete')->group(function () {
        Route::delete('/suppliers/{supplier}', [SupplierController::class, 'destroy'])->name('org.supplier.destroy');
    });
});
```

#### 6. 侧边栏菜单缓存优化

```php
// app/View/Components/SidebarMenu.php
class SidebarMenu extends Component
{
    public function render()
    {
        $menuConfig = cache()->remember(
            'sidebar:' . auth()->id(),
            now()->addHours(1),
            fn() => collect(config('menu'))
                ->map(function ($group) {
                    $group['children'] = collect($group['children'])
                        ->filter(fn($item) => auth()->user()->can($item['permission']))
                        ->values();

                    return $group['children']->isNotEmpty() ? $group : null;
                })
                ->filter()
                ->values()
        );

        return view('components.layout.sidebar', compact('menuConfig'));
    }
}
```

> 用户权限变更时清除缓存：`cache()->forget('sidebar:' . $user->id)`

### 组合方案数据流

```
┌─────────────┐     ┌──────────────┐     ┌───────────────┐     ┌─────────────┐
│ config/     │     │ Spatie       │     │ Blade @can    │     │ 路由中间件    │
│ menu.php    │────▶│ Permission   │────▶│ @canany       │────▶│ can:xxx      │
│ (菜单结构)   │     │ (权限数据)    │     │ (UI 隐藏)     │     │ (403 拦截)   │
└─────────────┘     └──────────────┘     └───────────────┘     └─────────────┘
     ▲                    ▲
     │                    │
     └──── 命名约定对齐 ────┘
      permission = org.supplier.view
      route     = org.supplier.index
      权限名与路由名严格同源
```

### 组合方案优势

| 优势 | 说明 |
|:---|:---|
| **零额外表** | 不新增任何数据库表，完全复用现有 Spatie Permission |
| **开发效率最高** | 新增页面只需：加菜单配置 + 加权限种子 + 加路由中间件 |
| **Laravel 原生** | `@can` / `@canany` / `can:` 中间件，无自定义逻辑 |
| **双保险** | UI 层隐藏 + 路由层拦截，即使绕过前端也返回 403 |
| **可缓存** | 菜单权限结果按用户缓存，页面渲染零额外查询 |
| **权限树复用** | 现有 140 权限树 + 9 角色直接可用，无需改造 |

### 新增页面操作清单

新增一个页面（如"仓库管理"）需要做以下 4 步：

1. **config/menu.php**：添加菜单项
   ```php
   ['key' => 'org.warehouse', 'label' => '仓库', 'route' => 'org.warehouse.index', 'permission' => 'org.warehouse.view']
   ```

2. **SystemDataSeeder**：添加权限
   ```php
   ['name' => 'org.warehouse.view',   'type' => 3],
   ['name' => 'org.warehouse.create', 'type' => 3],
   ['name' => 'org.warehouse.edit',   'type' => 3],
   ['name' => 'org.warehouse.delete', 'type' => 3],
   ```

3. **routes/web.php**：添加路由保护
   ```php
   Route::middleware('can:org.warehouse.view')->group(function () {
       Route::get('/warehouses', WarehouseList::class)->name('org.warehouse.index');
   });
   ```

4. **Blade 模板**：按钮加 `@can`
   ```blade
   @can('org.warehouse.create')
       <button type="button">新增仓库</button>
   @endcan
   ```

> 完成。无需建表、无需写 Service、无需改 Model。

> AI生成