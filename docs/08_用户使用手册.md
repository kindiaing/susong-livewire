---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '0ea0ac1c-71d1-4741-a39d-5e0e4c534863'
  PropagateID: '0ea0ac1c-71d1-4741-a39d-5e0e4c534863'
  ReservedCode1: '6597058b-198e-4026-a984-b73ff7a0e6b9'
  ReservedCode2: '6597058b-198e-4026-a984-b73ff7a0e6b9'
---

# 本地速送管理后台 — 用户使用手册

版本：V1.1.0
更新日期：2026-07-29
适用系统：本地速送管理后台（Laravel Livewire 桌面端）

---

## 1 系统概述

本地速送是一个本地生鲜配送服务平台，管理后台用于运营人员、财务人员、拣货员、配送司机等角色进行日常业务操作。

**访问地址**：https://livewire.test（开发环境）

**浏览器要求**：Chrome 90+ / Edge 90+ / Firefox 90+（仅桌面端，不支持移动端）

---

## 2 登录与退出

### 2.1 登录

1. 打开浏览器，访问系统地址
2. 在首页点击「登录」按钮，进入登录页
3. 输入用户名/手机号/邮箱和密码
4. 可勾选「记住登录状态」（7 天内免登录）
5. 点击「登录」按钮

> 连续 5 次登录失败后账户将被锁定 1 分钟。

**超级管理员初始账号**：

| 项目 | 值 |
| :--- | :--- |
| 用户名 | seeding |
| 密码 | Password |

### 2.2 退出

点击右上角用户名 → 选择「退出登录」，系统将返回首页。

---

## 3 导航布局

系统采用顶部水平导航栏布局：

```
┌──────────────────────────────────────────────────────────────┐
│  [Logo] 本地速送  │  [导航菜单...]  [Ctrl+K搜索]  │  🔔 ⚙️ │ 用户 ▼│
└──────────────────────────────────────────────────────────────┘
│                        内容区域                                │
```

- **左侧**：系统 Logo + 名称
- **中间**：导航菜单 + 命令搜索框
- **右侧**：通知铃铛、界面设置、用户菜单

### 3.1 快捷搜索（Ctrl+K）

按 `Ctrl+K` 打开命令面板，可快速搜索并跳转到任意功能页面。输入关键词后，系统实时过滤匹配的菜单项，按回车直接跳转。

---

## 4 功能模块说明

### 4.1 用户权限

#### 4.1.1 角色管理

**路径**：导航栏 → 用户权限 → 角色管理
**路由**：`/roles`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示所有角色，支持按名称搜索 |
| 新增角色 | 点击「新增」按钮，在弹窗中填写角色名称、显示名称、描述 |
| 编辑角色 | 点击行内「编辑」按钮，在弹窗中修改信息 |
| 删除角色 | 点击行内「删除」按钮，确认后删除（系统内置角色不可删除） |

系统预置 9 个角色：超级管理员、运营管理员、财务人员、拣货员、配送司机、商家、出纳、财务经理、运营经理。

#### 4.1.2 权限管理

**路径**：导航栏 → 用户权限 → 权限管理
**路由**：`/permissions`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示所有权限，按模块分组，树形展示 |
| 新增权限 | 点击「新增」按钮，选择模块、填写权限名称和标识 |
| 编辑权限 | 点击行内「编辑」按钮修改 |
| 删除权限 | 点击行内「删除」按钮确认删除 |

权限分为三级：模块级 → 页面级 → 按钮级。

---

### 4.2 组织主体

#### 4.2.1 供应商管理

**路径**：导航栏 → 组织主体 → 供应商管理
**路由**：`/suppliers`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示供应商名称、联系人、电话、状态，支持搜索 |
| 新增供应商 | 点击「新增」，填写基本信息和资质信息 |
| 编辑供应商 | 点击行内「编辑」修改供应商信息 |
| 禁用/启用 | 切换供应商状态，禁用后不可参与采购 |
| 删除 | 点击「删除」按钮确认删除 |

供应商状态：待审核 → 已启用 → 已禁用。

#### 4.2.2 商家管理

**路径**：导航栏 → 组织主体 → 商家管理
**路由**：`/merchants`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示商家名称、联系人、配送线路、状态 |
| 新增商家 | 填写商家基本信息、配送地址（支持多地址） |
| 编辑商家 | 修改商家信息 |
| 禁用/启用 | 切换商家状态 |
| 删除 | 确认删除 |

#### 4.2.3 配送线路

**路径**：导航栏 → 组织主体 → 配送线路
**路由**：`/delivery-routes`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示线路名称、覆盖区域、排序、状态 |
| 新增线路 | 填写线路名称、覆盖区域描述、排序 |
| 编辑线路 | 修改线路信息 |
| 禁用/启用 | 切换线路状态 |
| 删除 | 确认删除 |

#### 4.2.4 司机管理

**路径**：导航栏 → 组织主体 → 司机管理
**路由**：`/drivers`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示司机姓名、手机号、绑定车辆、状态 |
| 新增司机 | 填写司机基本信息 |
| 编辑司机 | 修改司机信息 |
| 禁用/启用 | 切换司机状态 |
| 删除 | 确认删除 |

#### 4.2.5 车辆管理

**路径**：导航栏 → 组织主体 → 车辆管理
**路由**：`/vehicles`

| 操作 | 说明 |
| :--- | :--- |
| 查看列表 | 显示车牌号、车型、载重、冷链标记、状态 |
| 新增车辆 | 填写车辆信息、载重、是否冷链 |
| 编辑车辆 | 修改车辆信息 |
| 禁用/启用 | 切换车辆状态 |
| 删除 | 确认删除 |

---

### 4.3 个人中心

**路径**：右上角用户菜单 → 个人中心
**路由**：`/profile`

#### 4.3.1 修改资料

修改姓名、手机号、邮箱等个人信息。系统会检测内容是否变更，未修改时不触发保存。

#### 4.3.2 修改密码

1. 输入当前密码
2. 输入新密码（8 位以上）
3. 确认新密码
4. 点击「保存」

密码修改后需重新登录。

---

### 4.4 系统管理

#### 4.4.1 系统配置

**路径**：导航栏 → 系统管理 → 系统配置
**路由**：`/settings`

系统配置按 6 组分类：

| 分组 | 配置项 | 说明 |
| :--- | :--- | :--- |
| 基本设置 | 站点名称、副标题、Logo | 系统基础信息 |
| 备案信息 | ICP 备案号、备案链接 | 底部版权栏显示 |
| 业务设置 | 起送金额、配送费规则 | 订单业务参数 |
| 通知设置 | 短信/邮件开关 | 通知渠道配置 |
| 安全设置 | 登录节流次数、锁定时长 | 安全策略 |
| 审核设置 | 审核节点开关 | 19 个审核节点独立开关 |

操作方式：点击配置项右侧编辑图标 → 行内修改 → 失焦自动保存（或按回车保存）。支持「重置默认值」恢复出厂配置。

#### 4.4.2 审核管理

**路径**：导航栏 → 系统管理 → 审核管理
**路由**：`/approval-config`

| 操作 | 说明 |
| :--- | :--- |
| 查看审核节点 | 显示 19 个审核节点，按模块分组 |
| 开关控制 | 点击开关启用/禁用某个审核节点 |
| 统计卡片 | 显示已启用/已禁用节点数量 |
| 模块筛选 | 按业务模块筛选审核节点 |

审核节点示例：采购单审核、供应商结算审核、充值审核、损耗审核等。

#### 4.4.3 审批列表

**路径**：导航栏 → 系统管理 → 审核管理 → 审批列表
**路由**：`/approvals`

| 操作 | 说明 |
| :--- | :--- |
| 待审批列表 | Tab 筛选：全部/待审批/已通过/已拒绝 |
| 查看详情 | 弹窗展示审批详情和数据快照 |
| 通过审批 | 点击「通过」按钮，确认后执行 |
| 拒绝审批 | 点击「拒绝」按钮，填写拒绝原因 |
| 撤回审批 | 申请人可撤回自己的审批 |

#### 4.4.4 操作日志

**路径**：导航栏 → 系统管理 → 操作日志
**路由**：`/operation-logs`

| 功能 | 说明 |
| :--- | :--- |
| 筛选 | 按操作人、操作时间、模块筛选 |
| 查看 | 显示操作人、操作类型、目标、时间、IP |
| 分页 | 支持分页浏览 |

所有写操作（增/删/改）自动记录，Livewire 内部请求不计入。

#### 4.4.5 审计日志

**路径**：导航栏 → 系统管理 → 审计日志
**路由**：`/audit-logs`

| 功能 | 说明 |
| :--- | :--- |
| 筛选 | 按动作类型、模型、操作人、日期范围筛选 |
| 数据快照 | 点击某条记录可弹窗查看修改前后的数据对比 |
| 分页 | 支持分页浏览 |

---

### 4.5 通知中心

**触发方式**：点击导航栏右侧铃铛图标

| 功能 | 说明 |
| :--- | :--- |
| Tab 筛选 | 全部/未读/系统/业务/审核 |
| 未读角标 | 铃铛上显示未读数量 |
| 标记已读 | 单条标记或一键全部标记已读 |
| 删除通知 | 单条删除 |

通知来源：系统提醒、业务通知、审批通知。

---

### 4.6 界面设置

**触发方式**：点击导航栏右侧齿轮图标

右侧滑出 Drawer 面板，包含：
- 通知偏好设置
- 个人中心快捷入口

---

## 5 首页

**路由**：`/`
**访问方式**：无需登录，公开访问

首页展示：
- 系统 Logo + 名称 + 描述
- 「登录」按钮
- 技术栈标签展示
- 底部版权栏：技术栈 | ICP 备案号 | 开发者

---

## 6 仪表盘

**路由**：`/dashboard`
**访问方式**：需登录

登录成功后的默认页面，展示系统运营概览（数据大屏功能待开发）。

---

## 7 待开发模块

以下模块的 Model 和页面正在开发中，导航菜单中已有入口但暂未链接到功能页面：

| 模块 | 包含功能 |
| :--- | :--- |
| 商品管理 | 分类、商品、SKU、条码、一品多供、可见性、关键词 |
| 采购管理 | 待采清单、采购单管理、采购退货 |
| 订单配送 | 客户订单、购物车、配送任务、签收存证、差异处理、售后退货 |
| 库存拣货 | 仓库管理、实时库存、库存日志、库存预警、拣货任务 |
| 财务管理 | 客户账户、充值、供应商结算、应收、发票、授权更正、价格策略、费用均摊、损耗管理 |

---

## 8 常见问题

### Q: 登录时提示"页面已过期"？
A: Session 已过期，页面会自动刷新，刷新后重新登录即可。

### Q: 修改配置后没有生效？
A: 系统配置修改后即时生效。如未生效，请尝试刷新浏览器（Ctrl+F5）。

### Q: 忘记密码怎么办？
A: 联系超级管理员重置密码。

### Q: 审核节点开关关闭后，还需要审核吗？
A: 关闭审核节点后，对应业务操作将跳过审核流程，直接执行。

### Q: 操作日志为什么有些操作没有记录？
A: 系统仅记录写操作（增/删/改），查询操作不计入。同时会过滤 Livewire 内部请求。

---

## 9 安装部署

详见《05_Setup_安装部署配置手册》。

快速安装命令：

```bash
# 仅内置数据（生产环境）
php artisan admin:install

# 含测试数据（开发环境）
php artisan admin:install --seed

# 清空重建
php artisan admin:install --reset --force
```

---

> AI生成